# P4 — Méta-analyse du harnais agentique (2026-09-17)

> Lecture seule ; shell limité à `git log/branch/status`, `ls`, `wc`. Branches auditées :
> kreadevis-backend `feat/lot-17-dockerization` (c517917), kreadevis-frontend `feat/lot-10-users-admin`
> (38d796c), deployment `chore/roadmap-sync-2026-07` (d0b3765, arbre sale), les autres sur `main`.
> VERIFIED = lu dans les fichiers ou git ; INFERRED = déduit, dont tout ce qui dépend de la version de Claude Code.

## 1. Inventaire

| Fichier source | Repo | Mode de chargement | ~lignes | Rôle (5 mots) |
|---|---|---|---|---|
| `~/.claude/CLAUDE.md` → `@RTK.md`, `@coding-conventions.md` | user | toujours (import) | 2 + 29 + 231 | règles transverses, rtk, lots |
| `~/.claude/settings.json` | user | déterministe (permissions, hooks, modèle) | 86 | `Bash(git *)` allow, hooks rtk/miroir |
| `~/.claude/hooks/sync-claude-agents.sh` | user | déterministe (PostToolUse Edit\|Write) | 30 | miroir CLAUDE.md ↔ AGENTS.md |
| `~/.claude/templates/project-skeleton/*` | user | sur invocation manuelle | 48 | squelette CLAUDE.md nouveau projet |
| `~/.claude/projects/<cwd>/memory/*` | user | toujours (index MEMORY.md, par cwd) | 5–15 | feedbacks (kb: 1, kf: 3, autres: 0) |
| `CLAUDE.md` | chaque repo | toujours (cwd) | 120–184 | conventions projet, gate LOTD, census |
| `AGENTS.md` | chaque repo | jamais par Claude Code ; toujours pour lecteurs AGENTS.md | = CLAUDE.md | miroir pour autres runtimes |
| `CONVENTIONS.md` | chaque repo | sur invocation (« READ FIRST », prose) | 211 (6 repos) / 231 (kb, kf) | copie du master, périmée ×6 |
| `.claude/settings.local.json` | kb, kf, mpb, elya, summerize | déterministe (permissions) | 12–61 | allow `git push`, `gh pr`, `git commit` |
| `kreadevis-frontend/.claude/CLAUDE.md` | kf | toujours (INFERRED, selon version) | 46 | règles Angular génériques |
| `.claude/skills/{sprint,lot-test,lot-audit,lot-ship,harness-sync,dep-update,i-have-adhd}/SKILL.md` + `checklists.md` | kb (branche lot-17 seulement) | sur invocation (Skill tool, découverts) | 944 | gate LOTD, procédures |
| `skill/*/SKILL.md` (7 fichiers) | kf, mpb, mpf, elya, elya-fe, summerize | jamais (non scanné) ; lecture manuelle | 350–1 000 | gate LOTD, invisibles au Skill tool |
| `lots.md` / `LOTS.md` / `dev-plan.md` | chaque repo | sur invocation (§9 : lecture au démarrage) | kb 1245, kf 724, elya 727, elya-fe 674, deploy 565, mpf 520, mpb 197, summerize 202 | scope, statut, historique |
| `ROADMAP.md` (master) + `~/ENV/projets/ROADMAP.md` (copie) | deployment / racine | sur invocation | 71 + 71 | statuts inter-repos (périmés) |
| `deployment/docs/runbook-septembre.md` | deployment | sur invocation | 91 | go/no-go fenêtre septembre |
| `deployment/docs/lots-remediation.md` | deployment (non suivi) | sur invocation | 852 | lots 19–21 / 13, 16 / C1–C4 |
| `.github/workflows/ci.yml` | 7 repos (pas deployment) | déterministe (push, PR) | 28–64 | build + tests ; image GHCR (kb seul) |
| git hooks (`.githooks/`, `.husky/`, lefthook, pre-commit) | aucun repo | — | 0 | absents partout |
| `prompt-harness.md` (racine) + copie `meal-planner-backend/` | racine, mpb | sur invocation (bootstrap) | 244 ×2 | générateur de harnais (périmé) |
| `claude-project-instructions.md` | racine | jamais (autre assistant) | 111 | protocole claude.ai, faits |
| `deployment/docs/audits/{run.sh,triage.sh,prompts/*,KNOWN-FINDINGS.md}` | deployment (non suivi) | sur invocation | 24 + 243 + 5×~40 | pipeline d'audit `claude -p` |
| `security.md`, `README.md`, `HELP.md`, `claude-md-context.txt` | kb, kf | sur invocation | 446 / 28–139 / 52 | docs projet |

Taille toujours chargée par repo (caractères ≈ tokens/4) — base user-level 17,7 K car. (≈ 4,4 k tokens) partout :

| Repo | + CLAUDE.md | Total chargé | Si §9 obéi (CONVENTIONS + lots) |
|---|---|---|---|
| racine `~/ENV/projets` | 0 (pas de CLAUDE.md) | ≈ 4,4 k tok | — |
| kreadevis-backend | 6,4 K | ≈ 6,0 k tok | + 16,7 K + 77 K ≈ +23 k tok |
| kreadevis-frontend | 9,8 K + 1,9 K (`.claude/CLAUDE.md`) | ≈ 7,3 k tok | + 16,7 K + 46 K ≈ +16 k tok |
| meal-planner-backend | 6,3 K | ≈ 6,0 k tok | + 13,9 K + 12,6 K ≈ +6,6 k tok |
| meal-planner-frontend | 8,7 K | ≈ 6,6 k tok | + 13,9 K + 22,5 K ≈ +9 k tok |
| elya | 9,2 K | ≈ 6,7 k tok | + 13,9 K + 44 K ≈ +14,5 k tok |
| elya-frontend | 11,6 K | ≈ 7,3 k tok | + 13,9 K + 41 K ≈ +13,7 k tok |
| deployment | 6,9 K | ≈ 6,1 k tok | + 13,9 K + 32,6 K + 3,3 K ≈ +12,5 k tok |

## 2. Matrice d'enforcement

| Invariant | Où c'est dit | Enforcé par | Mode de défaillance si le modèle l'ignore | Sévérité |
|---|---|---|---|---|
| 1 lot = 1 branche = 1 PR, stop humain après PR | `coding-conventions.md` §2.3/§2.9 (l.49–52), §7 ; `*/CLAUDE.md` Workflow (mpb:95–99, mpf:131–135, summerize:117–118) ; `prompt-harness.md:230` | **aucun** : `Bash(git *)` user-level (`settings.json:6`), `git push *`/`gh pr *`/`git commit *` projet (kb `settings.local.json:8–10`), pas de git hook, protection de branche non vérifiée | commit direct sur main, lots chaînés sans PR (mpf lots 02→13 linéaires ; mpb lot 12 sur branche lot 11) | bloquant |
| Skills invoqués via `Skill` à leur déclencheur (gate LOTD) | `coding-conventions.md` §13 (l.213–231) ; tables Skills des CLAUDE.md ; mémoire `feedback_skill_invocation.md` | **prose** ; découverte automatique seulement si `.claude/skills/` (kb, branche non mergée) | gate exécuté « de mémoire », étapes sautées (documenté dans la mémoire kb) | bloquant |
| Pas de push/PR sans `docs/audits/lot-XX.md` sans Critical | `*/CLAUDE.md` bloc ⛔ (kb:57–59, kf:46–48…) ; §10.6 ; `lot-ship/SKILL.md:117` | **prose** (checklist lot-ship) | lots ✅ sans rapport (mpf : 12 lots, 0 rapport) | majeur |
| Front validé contre backend réel avant merge | `ROADMAP.md:26,39–40` ; `kreadevis-frontend/lots.md:46,76–90` (lot 0) | **aucun** (CI kf = tests unitaires seuls, `ci.yml:27–28`) | lots 1–7 mockés → lot 7b correctif ; lots 8–10 mergés/en PR avec lot 0 toujours ⬜ | bloquant |
| Statut tenu à jour (lots + ROADMAP) | §2.1 (l.44–46) ; `prompt-harness.md:67–69` ; `ROADMAP.md:3` ; `claude-project-instructions.md:15` | **aucun** (édition manuelle) | ROADMAP faux sur 3 repos, master non mergé et sale | bloquant |
| Validation gate verte avant chaque commit | §2.6, §10.4 ; `*/CLAUDE.md` Validation gate | **CI post-hoc** (push/PR, déterministe mais après coup) ; pas de pre-commit | commits rouges sur branche, détectés au push | majeur |
| Seuil de couverture ratchet, jamais baissé/désactivé | `lot-test/SKILL.md:107–113` ; kb CLAUDE.md:61–67 ; mpf CLAUDE.md:52–57 | **build** pour le seuil (`pom.xml` kb:257 = 0.70, mpb:191 = 0.80, elya:178 = 0.80 ; `angular.json` kf:89 = 79, elya-fe:83 = 80 ; karma mpf:27 = 0) ; **rien** sur les exclusions | seuil contourné par `<excludes>` de packages métier (mpb `auth/**`, `planning/**`, `shopping/**`) | majeur |
| Revue sécurité indépendante (subagent `security-review`) | `lot-audit/SKILL.md:44–49` (kb, kf, tous) | **aucun** : type de subagent inexistant, fallback checklist | jamais exécutée comme spécifié (`docs/audits/lot-16.md:5–7`) | majeur |
| Schéma additif : nouvelle migration, jamais éditer une migration mergée ; expand/contract | kb CLAUDE.md:37 ; mpb CLAUDE.md:98,109 ; elya CLAUDE.md:39 ; kb lots.md:21–26 (C3) | **aucun** (ni CI ni hook) — respecté à ce jour (git : migrations toujours en `A`) | rollback d'image sur schéma migré (K4), perte de données | majeur |
| `AGENTS.md` = `CLAUDE.md` octet pour octet | §12 (l.198–203) ; §10.5 | **hook** PostToolUse Edit\|Write (`settings.json:38`) — pas Bash, pas hors Claude Code | divergence silencieuse après `sed -i`/`cp` ou édition IDE | mineur |
| `CONVENTIONS.md` = master | §12 (l.205–211) ; `harness-sync/SKILL.md:35–38` | **prose** (`cmp` manuel) | 6 repos à 211 lignes sans §13 depuis 2026-08-12 | majeur |
| Jamais `--no-verify`, jamais force-push, jamais commit sur main | §7 (l.126) ; §4 ; `lot-ship/SKILL.md:115` | **aucun** (`Bash(git *)` allow) | pas de prompt de permission, exécution directe | majeur |
| Tests par lot (unitaire par méthode publique, intégration par endpoint) | §2.5, §3 ; `lot-test` matrice | **partiel** (seuil de couverture) | endpoints sans test si couverture globale tient | majeur |
| Commits Conventional, anglais, scope = lot | §7 ; `lot-ship/SKILL.md:22–55` ; §10.3 | **aucun** (pas de commitlint) | messages mixtes FR/EN (kf `1af73b6`) | mineur |
| Fichier de lots jamais modifié sans accord (statut excepté) | `lot-audit/SKILL.md:150–152,160` ; `harness-sync:108` | **prose** | scope creep silencieux | mineur |
| Secrets via env, `.env` ignoré, fail-fast | §5 ; `*/CLAUDE.md` Secrets | **`.gitignore`** (déterministe, présent partout) + `@Validated` (kb lot 16) | — (tenu) | — |
| Census des documents à jour | §12 ; `harness-sync:40–42` | **prose** | fichiers listés non suivis (elya-fe), skills absents de la table | mineur |
| Dépendance/modèle de données : demander avant | §4 (l.90–97) | **prose** | — | mineur |

## 3. Constats

| # | Sévérité | Constat | Preuve (file:line) | Tag | Pourquoi ça compte | Changement en une ligne |
|---|---|---|---|---|---|---|
| 1 | bloquant | Les skills de 6 repos sur 7 vivent dans `skill/`, répertoire que le Skill tool ne scanne pas ; kreadevis-backend les a relocalisés dans `.claude/skills/` sur une branche non mergée uniquement (commit `dafa950`, 2026-08-23, contenu dans `feat/lot-17` et `chore/relocate-skills-to-claude-dir`, absent de `main`) | `kreadevis-frontend/CLAUDE.md:37–42` ; `meal-planner-backend/CLAUDE.md:62–67` ; `elya/CLAUDE.md:112–117` ; `elya-frontend/CLAUDE.md:39–44` ; `meal-planner-frontend/CLAUDE.md:100–105` ; `summerize-youtube/CLAUDE.md:93–98` ; `elya/CONVENTIONS.md:196` (copie 211 l. dit encore `skill/*/SKILL.md`) | VERIFIED (dans cette session, seuls les skills kb ont été découverts) | §13 rend l'invocation obligatoire ; un modèle faible ne lit jamais spontanément 7 fichiers non exposés — le gate LOTD est joué « de mémoire », ce que la mémoire `feedback_skill_invocation.md` documente déjà | `git mv skill .claude/skills` dans 6 repos + census ; merger `chore/relocate-skills-to-claude-dir` |
| 2 | bloquant | Le « stop humain » après PR et les interdits git (`--force`, `--no-verify`, commit sur main) ne sont gardés par aucune permission ni hook : `git *` autorisé globalement, `git push *`, `git commit *`, `gh pr *` autorisés par projet ; aucun `.githooks/`, `.husky/`, lefthook, pre-commit | `~/.claude/settings.json:6` ; `kreadevis-backend/.claude/settings.local.json:8–10,23` ; `elya/.claude/settings.local.json:5` ; `ls` des 8 repos | VERIFIED | Toute la couche « déterministe » de §7/§10 est en réalité comportementale ; sur DeepSeek c'est le point le plus probable de dérive | Retirer ces allow, ajouter `deny` sur `git push --force*`/`--no-verify`, hook `pre-push` versionné |
| 3 | bloquant | `ROADMAP.md` « single source of truth » est périmé et son master n'est ni mergé ni propre : branche `chore/roadmap-sync-2026-07` (d0b3765, 2026-07-14) avec `CLAUDE.md`, `AGENTS.md`, `LOTS.md` modifiés et `docs/audits/`, `docs/lots-remediation.md` non suivis. Statuts faux : KB « 12b,13,14,15,16 🔲, SB41 🔄 » alors que tout est ✅ depuis 08-05→08-12 ; KF « 8, 9 🔲 » alors que mergés 09-04 ; MP-BE « 12 🔄 PR #16 » alors que mergé 08-05 | `ROADMAP.md:21,26,37` ; `kreadevis-backend/lots.md:48–53` ; `kreadevis-frontend/lots.md:55,57` ; `meal-planner-backend/dev-plan.md:187` ; `git status` deployment ; `git log --first-parent main` kb, kf | VERIFIED | Le go/no-go du runbook (`runbook-septembre.md:14–17`) et l'instruction « lire ROADMAP en premier » (`claude-project-instructions.md:15`) reposent sur ces statuts | Merger + committer deployment ; faire dériver le statut d'un script `git log` plutôt que d'éditions manuelles |
| 4 | bloquant | Le gate « front validé contre backend réel » est ignoré une seconde fois : KF lot 0 toujours ⬜ (ré-ouvert par l'audit du 2026-07-10) alors que 7b/8/9 sont mergés et 10 en PR ; MP front↔back jamais exercé ; aucune CI d'intégration | `kreadevis-frontend/lots.md:46,76–90` ; `kreadevis-frontend/.github/workflows/ci.yml:27–28` ; `ROADMAP.md:39–40` ; `meal-planner-frontend/lots.md:71` ; `kreadevis-backend/lots.md:62–64` | VERIFIED | C'est exactement le piège qui a produit le lot 7b (`e3cb529`, `02e3281`) ; il se rejoue sur 8→10 | Job CI « contract » (compose backend + smoke) obligatoire sur `feat/lot-*` KF |
| 5 | majeur | Deux règles opposées coexistent, parfois dans le même fichier : « PR ouverte → stop, ne pas démarrer le lot suivant avant merge » vs « chaque lot part de la branche du précédent sans attendre le merge » | `coding-conventions.md:52` ; `elya/CLAUDE.md:64` vs `:138–139` ; `meal-planner-frontend/CLAUDE.md:12` vs `:126–127` ; `kreadevis-backend/.claude/skills/sprint/SKILL.md:69–72` ; mémoire kf `feedback_pr_workflow.md` | VERIFIED | Un modèle faible tranche au hasard ; git montre 3 lots posés le même jour sur kb (13, 14, 15 le 2026-08-06) | Garder §2.9 ; passer `sprint` en invocation explicite (`disable-model-invocation: true`) et retirer le paragraphe « Sprint chaining » des CLAUDE.md |
| 6 | majeur | `CONVENTIONS.md` périmé sur `main` dans 6 repos (211 lignes, sans §13 « skill invocation ») vs master 231 ; branches `chore/sync-conventions-md` non mergées depuis 2026-08-12 | `wc` ; `elya/CONVENTIONS.md:180–211` (fin sans §13) ; `git log -1 main -- CONVENTIONS.md` : elya `7fb4ea2` 07-10, mpb `1c446a7`, mpf `587a4a4`, deployment `e60352a`, elya-fe `fcbfbfb`, summerize `0ee166d` | VERIFIED | Pour tout runtime qui ne charge pas `~/.claude` (Cursor natif, lecteurs AGENTS.md) et pour un agent qui obéit à « READ CONVENTIONS.md FIRST », §13 n'existe pas | Merger les 6 branches ; `cmp` en CI |
| 7 | majeur | L'étape 2 de `lot-audit` (subagent `security-review`) est inexécutable : ce type de subagent n'existe pas ; le fallback est une checklist en prose | `kreadevis-backend/.claude/skills/lot-audit/SKILL.md:44–49` ; `kreadevis-frontend/skill/lot-audit/SKILL.md:44–49` ; `kreadevis-backend/docs/audits/lot-16.md:5–7` | VERIFIED (ici `security-review` est un skill, pas un agent) | La seule étape « indépendante » du gate n'a jamais tourné telle que spécifiée | Remplacer par `Skill(security-review)` ou définir `.claude/agents/security-review.md` |
| 8 | majeur | Gate de couverture creux sur meal-planner : JaCoCo exclut les packages métier `auth/**`, `planning/**`, `shopping/**` tout en affichant 0.80 ; le frontend impose un seuil 0 tout en annonçant « coverage gate enforced » | `meal-planner-backend/pom.xml:157–159,191` ; `meal-planner-frontend/karma.conf.js:27` ; `meal-planner-frontend/CLAUDE.md:159` | VERIFIED | « Jamais baisser le seuil » est contourné par les exclusions, invisibles à qui lit le seuil | Interdire l'exclusion de packages métier ; lister les exclusions dans CLAUDE.md ; revue de `<excludes>` dans la checklist lot-audit |
| 9 | majeur | elya-frontend : squelette Angular complet et 6 skills non versionnés sur `main`, aucune branche ; le census liste des fichiers non suivis ; ROADMAP dit « 0–12 🔲 » | `git status` elya-frontend (`?? angular.json src/ package.json skill/dep-update/ …`) ; `elya-frontend/CLAUDE.md:178–183` ; `ROADMAP.md:34` | VERIFIED | Du travail existe hors de tout lot/branche/PR : le workflow n'a pas été appliqué du tout ; risque de perte | Lot 0 = commit sur `feat/lot-0-scaffold` + PR |
| 10 | majeur | meal-planner-frontend : lots 02→13 posés linéairement sur `main` (seuls merges #1 et #16), zéro rapport d'audit pour 12 lots ✅ ; meal-planner-backend : le lot 12 est commité sur la branche du lot 11 et réparti sur 3 branches | `git log --first-parent main` mpf ; `meal-planner-frontend/docs/audits/` (`.gitkeep` seul) ; `meal-planner-frontend/lots.md:68` ; `git log --all --not main` mpb (`origin/lot-11-shopping-export` tip = `feat(12)` `8957603`) | VERIFIED | Les invariants ont cédé dès qu'ils n'étaient que de la prose (harnais ajouté après coup : `5eea6aa` 2026-07-09) ; le pattern se reproduira sur elya-frontend | Cf. #2 : hook `pre-push` exigeant `docs/audits/lot-XX.md` pour toute branche `*lot-XX*` |
| 11 | majeur | « Migration additive, jamais éditer une migration mergée » n'a aucune vérification (ni CI ni hook) ; respecté à ce jour (toutes les migrations sont en `A`) | `kreadevis-backend/CLAUDE.md:37` ; `meal-planner-backend/CLAUDE.md:98,109` ; `elya/CLAUDE.md:39` ; `kreadevis-backend/lots.md:21–26` ; `git log --name-status -- src/main/resources/db` (kb, mpb, elya) | VERIFIED | Seul invariant à conséquence irréversible (K4 : rollback d'image sur schéma migré) | Job CI : `git diff --name-status origin/main -- src/main/resources/db` → échec si autre chose que `A` |
| 12 | majeur | Le hook miroir ne couvre que `Edit\|Write` : éditions via Bash (`sed -i`, `cp`, heredoc) et tout runtime hors Claude Code le contournent ; le `cmp` du §10.5 reste de la prose | `~/.claude/settings.json:38` ; `~/.claude/hooks/sync-claude-agents.sh:19–28` ; usage manuel `cp CLAUDE.md AGENTS.md` (`meal-planner-frontend/.claude/settings.local.json:14`) | VERIFIED (matcher) / INFERRED (IDE) | Le miroir est la seule voie vers Cursor/AGENTS.md ; une divergence y est silencieuse | Étendre le matcher à `Bash` (détection des deux noms) ou `cmp` en CI |
| 13 | majeur | §9 impose à chaque démarrage de relire `CONVENTIONS.md` (déjà chargé par import) puis tout le fichier de lots : 77 K car. sur kb, 46 K kf, 44 K elya, dont l'historique des lots ✅ avec SHA | `coding-conventions.md:105–116` ; `wc` ; `kreadevis-backend/lots.md:79–140` (lots 1–4 terminés, détaillés) | VERIFIED | ≈ 25 k tokens avant la première action sur kb ; la fidélité de DeepSeek aux instructions décroît avec la longueur | Scinder `lots.md` (à faire) / `lots-done.md` (archive) ; §9 ne lit que la section du lot courant |
| 14 | majeur | Le générateur de harnais prescrit encore `skill/<name>/SKILL.md` et `lot-XX-slug`, contre §12 (`.claude/skills`) et §7 (`feat/lot-N-slug`) ; copie identique dans meal-planner-backend | `prompt-harness.md:96,109,128,188` ; `:101,230` ; `wc -c` (13 796 o ×2) | VERIFIED | Chaque nouveau repo bootstrappé reproduit #1 et le conflit de nommage #24 | Corriger 4 lignes du master, supprimer ou resynchroniser la copie |
| 15 | mineur | Contradictions internes kb : `sprint` exige « ≥ 80 % » là où `lot-test` et CLAUDE.md posent un ratchet à 70 % ; `harness-sync` parle encore du répertoire `skill/` après relocalisation | `kreadevis-backend/.claude/skills/sprint/SKILL.md:63,84` vs `lot-test/SKILL.md:5,16` et `CLAUDE.md:63` ; `harness-sync/SKILL.md:5–6,29,41,78` | VERIFIED | Un arrêt de sprint sur « coverage < 80 % unreachable » est faux pour ce repo | 5 lignes |
| 16 | mineur | Skills meal-planner-backend rédigés en français, contre §11 (docs agent en anglais) | `meal-planner-backend/skill/lot-test/SKILL.md:3–30` ; `skill/lot-ship/SKILL.md:3–25` | VERIFIED | Fichier mixte FR/EN = pire cas documenté en §11 pour DeepSeek | Régénérer depuis kb ou traduire |
| 17 | mineur | `elya/CLAUDE.md` périmé : « Prod: Docker on Raspberry Pi », « Angular 21 » ; `elya-frontend/CLAUDE.md` « Angular 21 » ; `elya-frontend/lots.md` cible arm64/Pi — contre les décisions ROADMAP (G6, Angular 22) | `elya/CLAUDE.md:18,57,73,106` ; `elya-frontend/CLAUDE.md:20` ; `ROADMAP.md:34,70–71` ; `p3-deployability-2026-09-17.md:151–153` | VERIFIED | Un agent elya prendra la mauvaise cible de prod et la mauvaise version Angular | `harness-sync` ciblé sur les deux repos |
| 18 | mineur | kreadevis-frontend charge deux fichiers d'instructions (`CLAUDE.md` + `.claude/CLAUDE.md`, 46 l. de règles Angular génériques) ; la note archivée prévoyait de réduire le second à une délégation, jamais fait ; la note (en français) est listée au census | `kreadevis-frontend/.claude/CLAUDE.md:1–46` ; `kreadevis-frontend/claude-md-context.txt:29–48` ; `kreadevis-frontend/CLAUDE.md:168` | VERIFIED (contenu) / INFERRED (chargement simultané selon version) | Deux sources sans hiérarchie, contradiction signals/RxJS déjà rencontrée | Remplacer `.claude/CLAUDE.md` par une ligne de délégation |
| 19 | mineur | Outils d'audit mal placés : `run.sh` attend `audit/prompts`, `audit/out`, `audit/KNOWN-FINDINGS.md` sous `~/ENV/projets` ; le dossier racine `audit/` est vide, les fichiers vivent sous `deployment/docs/audits/` (non suivis) | `deployment/docs/audits/run.sh:2,6,7,10` ; `deployment/docs/audits/triage.sh:4–7,10` ; `ls audit/` | VERIFIED | Le pipeline `claude -p` documenté ne tourne pas tel quel | Symlink `audit → deployment/docs/audits` ou corriger 4 chemins |
| 20 | mineur | Instructions adressées à un autre assistant dans l'arbre : protocole ROADMAP UPDATE / REPRISE, « Sonnet escalation », `--backend anthropic` alors que `settings.json` et `run.sh` pointent fable ; non chargé (pas de CLAUDE.md racine) | `claude-project-instructions.md:9,34–44,88` ; `~/.claude/settings.json:23` ; `deployment/docs/audits/run.sh:3,5` | VERIFIED | Risque seulement si un agent le lit (les prompts d'audit le demandent) : adoption du protocole REPRISE | En-tête « not for coding agents » ou déplacer hors de l'arbre |
| 21 | mineur | §10.1 « relire `feedback_*.md` » dépend du cwd : mémoire par répertoire ; feedbacks présents seulement pour kb (1) et kf (3), aucun pour elya, meal-planner, deployment ; une session ouverte à la racine (IDE) n'en voit aucun | `ls ~/.claude/projects/-home-selim-ENV-projets*/memory` | VERIFIED (contenu) / INFERRED (portée cwd) | La règle est un no-op sur 5 repos et depuis la racine | Promouvoir les 4 feedbacks dans CONVENTIONS ou les CLAUDE.md concernés |
| 22 | mineur | `deployment/CLAUDE.md` décrit une arborescence `stacks/core|elya|…` inexistante ; `stacks/` ne contient que `exercice-zenika/` et `quarkus-startup/` vides ; la validation gate `find stacks …` passe à vide | `deployment/CLAUDE.md:70,75–90` ; `ls -R deployment/stacks` | VERIFIED | Gate trivialement vert, doc « cible » présentée comme réelle (contre `prompt-harness.md:116–118`) | Retirer les 2 dossiers, marquer l'arborescence « cible » |
| 23 | mineur | `i-have-adhd` listé au census (« enlight llm return ») mais absent des tables Skills ; sur kf le dossier n'est pas suivi | `kreadevis-backend/CLAUDE.md:119` vs `:42–49` ; `meal-planner-backend/CLAUDE.md:132` ; `elya/CLAUDE.md:177` ; `elya-frontend/CLAUDE.md:177` ; `git status` kf (`?? skill/i-have-adhd/`) | VERIFIED | Invariant « table Skills ⇔ répertoire » (`summerize-youtube/CLAUDE.md:100`) violé partout | Une ligne par repo |
| 24 | mineur | Nommage de branches divergent : §7 `feat/lot-N-slug` vs meal-planner `lot-XX-slug` ; CI kb ne se déclenche que sur `feat/**`, `chore/**` (une branche `fix/…` existe) | `coding-conventions.md:130–131` ; `meal-planner-backend/CLAUDE.md:95` ; `meal-planner-frontend/CLAUDE.md:131` ; `kreadevis-backend/.github/workflows/ci.yml:5` ; branche `fix/audit-corrections` | VERIFIED | Push sur une branche hors motif = pas de CI avant la PR | `branches: ["**"]` ou aligner le nommage |
| 25 | mineur | Message de commit mixte FR/EN sur `main` kf, aucun commitlint | `kreadevis-frontend` `1af73b6` « docs: sync lots.md status - mark lot 8 done, recadrer lot 9 (facture retirée) » ; `coding-conventions.md:118–128` | VERIFIED | La règle la plus répétée du harnais (§7, §10.3, lot-ship) n'a que la prose | Hook `commit-msg` regex (5 lignes) |

## 4. Top 5 changements (risque retiré / effort)

1. **Relocaliser `skill/` → `.claude/skills/` dans 6 repos et merger les 3 familles de branches en attente** (`chore/relocate-skills-to-claude-dir`, `chore/sync-conventions-md` ×6, `chore/roadmap-sync-2026-07`) — #1, #6, #3, #14 ; ~30 min ; rend §13 mécaniquement possible.
2. **Hook `pre-push` versionné (`.githooks/` + `git config core.hooksPath`) + purge des allow** : refuse push sur `main`, exige `docs/audits/lot-XX.md` pour `*lot-XX*`, regex Conventional Commits ; retirer `git push *`, `git commit *`, `gh pr *` des `settings.local.json`, remplacer `Bash(git *)` par des motifs lecture seule et `deny` `--force`/`--no-verify` — #2, #10, #25.
3. **Job CI « migrations immuables »** (échec si un fichier sous `src/main/resources/db` n'est pas en `A` vs `origin/main`) — #11 ; 10 lignes YAML ; seul invariant irréversible.
4. **Job CI d'intégration front↔back** sur `feat/lot-*` des frontends (compose backend + smoke login→devis→PDF) et règle lot-ship « ROADMAP mis à jour dans le même commit que `docs(N): mark lot N done` » — #4, #3.
5. **Trancher sprint vs stop et alléger le démarrage** : retirer « Sprint chaining » des CLAUDE.md, `disable-model-invocation: true` sur `sprint`, archiver les lots terminés hors de `lots.md` — #5, #13 ; lecture de démarrage divisée par 3.

## 5. À ne pas toucher

- **`CONVENTIONS.md` dans chaque repo**, malgré le doublon avec l'import `~/.claude/CLAUDE.md` : unique vecteur des règles pour les lecteurs `AGENTS.md` (Cursor natif, collaborateurs) et pour toute session où le user-level ne serait pas chargé. Le problème est la copie périmée (#6), pas la copie.
- **Le bloc ⛔ LOTD répété dans chaque `CLAUDE.md`** (doublon apparent de §13 et des skills) : c'est la seule règle que voit un modèle faible dans le fichier de premier niveau ; la répétition est délibérée (`claude-md-context.txt:3–8` documente l'oubli en session longue).
- **`docs/audits/lot-XX.md` commités sur la branche du lot** et les commits `docs(N): mark lot N done` : seule preuve déterministe qu'un gate a tourné (`git log -- docs/audits`), à ne pas remplacer par une note de PR.
- **`checklists.md` de `lot-audit`** : c'est le fallback qui a réellement tourné (`lot-16.md:5–7`) ; le retirer au motif que le subagent « couvre » laisserait le gate vide (#7).
- **`.gitignore` (`*.local.md`, `.env`) et `.env.example`** : unique enforcement déterministe de §5/§8, présent et correct dans les 8 repos.
- **Les mémoires `feedback_*.md`** (skill invocation, PR workflow, no-signals, en dash) : trace des échecs passés ; à promouvoir (#21), pas à supprimer.
- **`sync-claude-agents.sh`** : incomplet (#12) mais seul hook déterministe de l'invariant miroir ; l'étendre, pas le remplacer par de la prose.

## 6. Non vérifié

- **Protection de branche GitHub et existence des PR** pour kb lots 11→16 et kf 7b→9 : shell restreint (pas de `gh`) ; des commits linéaires sans merge sont compatibles avec « rebase and merge » comme avec un push direct.
- **Configuration deepclaude** : `~/.claude/settings.deepseek.bak` non ouvert (peut contenir un jeton) ; `run.sh:11` suggère un routage par `ANTHROPIC_BASE_URL`/`AUTH_TOKEN`/`MODEL` ; précédence env vs `settings.json:23` non vérifiée. Aucun `deployment/tools/` vendoré.
- **Chargement des hooks, skills et permissions depuis le plugin IntelliJ et l'extension Cursor** : dépend de la version ; non testable ici. Un agent Cursor natif ne lirait que `AGENTS.md` (+ `CONVENTIONS.md` s'il obéit), sans hook ni skill ni permission.
- **Chargement simultané de `.claude/CLAUDE.md` et `CLAUDE.md`** (#18) dans la version courante.
- **Contenu de `skill/` sur `main` de kreadevis-backend** : déduit de `git log` (ajout `709f125`, aucune suppression sur `main`), pas de `git ls-tree` (hors liste autorisée).
- **Résultat réel des gates** (`./mvnw verify`, `npm test`) et taux de couverture effectifs : non exécutés.
- **Contenu du diff sale de deployment** (`git diff` interdit) : seuls les noms de fichiers sont connus.
- **Legacy `kreadevis/` et `exercices/`** : hors périmètre ; summerize-youtube lu pour la dérive seulement (skills courts, en `skill/`, mêmes constats #1/#23).
